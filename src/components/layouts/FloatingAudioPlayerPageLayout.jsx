const FloatingAudioPlayerPageLayout = ({ children }) => {
    return (
        <div className="mt-[48px] mb-[110px] p-4 w-full h-full flex flex-col items-center justify-start">
            {children}
        </div>
    );
};

export default FloatingAudioPlayerPageLayout;
